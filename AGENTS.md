# AGENTS.md

## 项目概述

`VectorDAQ Testlab UI` 是一个独立运行的前端演示项目，用来模拟实验室/台架测试场景下的 DAQ（数据采集）与 NVH（噪声、振动与声振粗糙度）分析界面。

项目的核心展示内容是一个基于 Three.js 的 3D waterfall 图，用 RPM、Frequency 和 Amplitude 三个维度来表现一组模拟生成的频谱/阶次分析数据。整体界面采用暗色工业风，围绕“采集、监测、分析、告警”这一类测试软件的使用场景组织。

这个仓库目前不是一个框架型工程，没有 `package.json`、构建工具或打包流程，本质上是：

- 一个静态 HTML 页面
- 一个样式文件
- 一个前端逻辑脚本
- 一个本地 vendored 的 Three.js
- 一个极简 Node 静态文件服务器
- 一个 Playwright 可视化验证脚本

## 主要用途

从现有代码来看，这个项目更像是以下几类用途之一：

- 设计/原型展示：用于演示工业测试软件的界面风格和信息架构
- 前端视觉样例：展示高密度仪表类 UI 与 3D 图表结合的效果
- Three.js 图形实验：验证 waterfall 图、交互光标和辅助信息联动的实现方式
- 可视化回归样本：通过截图和像素统计做基础自动验证

## 目录与文件职责

### `index.html`

负责页面结构，是整个界面的骨架。主要分区包括：

- 顶部工具栏：品牌、控制按钮、系统状态
- 左侧面板：项目树、Run Matrix、通道健康信息
- 中央工作区：waterfall 图、图表标题、读数区、底部频谱和事件日志
- 右侧面板：分析值、阶次跟踪、光标坐标、Limit State
- 底部状态栏：流缓冲、负载、温度、校准到期时间

### `styles.css`

负责整体视觉样式，特点比较明确：

- 深色背景 + 青蓝色系工业风高亮
- 以 grid 为主的整体布局
- 高密度信息面板风格
- 大量使用等宽字体展示“设备/测量/状态”类信息
- 组件更偏仪表软件，而不是营销网站

### `app.js`

这是项目最核心的逻辑文件，承担了几件事情：

- 初始化 Three.js 场景、相机、灯光、渲染器
- 生成模拟 waterfall 数据
- 构建曲面、线框、坐标网格、峰值点云
- 处理鼠标交互：拖拽旋转、滚轮缩放、移动更新光标位置
- 根据当前光标更新各种读数
- 在底部 canvas 上绘制当前 slice 的 mini spectrum
- 通过 `requestAnimationFrame` 持续刷新画面与读数

### `vendor/three.module.min.js`

本地保存的 Three.js 模块文件，前端直接通过相对路径导入，没有依赖外部 CDN，也没有包管理器参与。

### `server.mjs`

一个非常轻量的 Node 静态文件服务器。

作用：

- 默认监听 `127.0.0.1:4173`
- 把 `/` 映射到 `index.html`
- 按扩展名返回基础 MIME 类型
- 做了简单的路径规范化和目录访问拦截

这意味着项目的运行方式非常直接，不依赖 Vite、Webpack 或其他开发服务器。

### `verify.cjs`

这是当前仓库里的“测试脚本”，严格说更接近“可视化验证脚本”。

它会：

- 用 Playwright 启动 Chromium
- 打开本地页面 `http://127.0.0.1:4173`
- 等待 waterfall canvas 渲染完成
- 生成桌面和移动端截图
- 裁切图表区域截图
- 用 `pngjs` 对截图做简单像素统计
- 校验 WebGL 是否可用、画布尺寸是否合理、颜色分布是否达到阈值、移动端是否有溢出元素

只要这些条件通过，脚本就会以退出码 0 结束。

## 运行方式

项目运行依赖 Node.js，但不依赖 npm 工程结构。

启动方式：

```sh
node server.mjs
```

访问地址：

```txt
http://127.0.0.1:4173
```

## 测试方式

在本地服务启动后执行：

```sh
node verify.cjs
```

验证通过后，仓库根目录会生成这些截图：

- `verification-desktop.png`
- `verification-chart.png`
- `verification-mobile.png`

仓库里另外还存在：

- `verification-fullpage.png`
- `verification-iab.png`

从命名看，它们更像是之前留下的验证产物，不是当前 `verify.cjs` 直接生成的文件。

## 当前测试结论

我最近一次实际运行时，测试是通过的，观察到的结果包括：

- 页面无 console error
- WebGL 可正常创建
- 主图 canvas 尺寸正常
- 图表截图亮度与颜色分布达到脚本阈值
- 移动端视口下没有检测到文字/组件溢出

说明当前版本至少在本地静态运行和基础视觉回归层面是健康的。

## 数据与交互机制

### 数据来源

当前项目没有后端、没有真实采集设备接入，也没有从文件读取实验数据。

waterfall 数据是前端在 `app.js` 中通过算法实时生成的模拟数据，主要由这些成分构成：

- 宽带底噪
- 低频抬升
- 1x / 2x / 3x / 6.5x / 9x 等阶次 ridge
- 结构共振峰
- blade pass 类峰值

所以这个项目目前是“分析界面模拟器”，不是“真实 DAQ 客户端”。

### 交互行为

当前主要交互有：

- 鼠标移动：更新光标所对应的频率/RPM 索引
- 鼠标拖拽：改变相机轨道角度
- 鼠标滚轮：缩放观察距离
- 鼠标离开图表：恢复自动游走光标

当光标位置变化时，会同步更新：

- 顶部 RPM / FREQ / AMP 读数
- 图表上的 cursor tag
- 右侧分析值与坐标值
- 底部 mini spectrum

## UI 内容性质

页面中许多内容是“看起来像真实系统”的静态或半动态展示数据，例如：

- Project Explorer 的项目与通道名称
- Channel Health 数值
- Event Log
- Limit State
- 状态栏中的负载、温度、校准信息

这些内容大多是硬编码或由局部公式驱动的展示值，不代表系统已具备完整业务逻辑。

## 当前工程特点

这个仓库有几个明显特点：

- 轻量：没有复杂依赖管理和构建链
- 自包含：Three.js 本地 vendored，静态资源直接可用
- 可演示：打开即见完整界面效果
- 可验证：附带 Playwright 截图校验
- 偏原型：业务数据和状态机并不完整，更强调视觉与交互表现

## 已知注意事项

### 1. 不是标准 npm 项目

没有 `package.json`，因此不能直接使用常见的 `npm run dev`、`npm test` 之类命令。项目更适合用 `node server.mjs` 和 `node verify.cjs` 直接运行。

### 2. 测试依赖本机 Playwright 浏览器

`verify.cjs` 依赖 Playwright 的 Chromium 可执行文件。如果环境里尚未安装对应浏览器，需要先安装后才能跑通验证。

### 3. 测试属于视觉验证，不是单元测试

当前没有：

- 单元测试
- 组件测试
- 真实数据契约测试
- 后端接口测试

现有测试重点是“页面是否正常渲染、3D 图是否非空、移动端是否溢出”。

### 4. README 已补充

这个仓库的 `README.md` 已经补充过中文化的项目说明、结构说明、启动方式和测试方式。`AGENTS.md` 可以视为更偏“内部交接/项目理解”的版本。

## 适合后续扩展的方向

如果后面继续演进，这个项目很自然可以往这些方向扩展：

- 接入真实采集数据或录波文件
- 把静态面板改成可配置的数据模型
- 引入更完整的图表控制（视角重置、通道切换、marker 管理）
- 增加阶次筛选、频段选择、报警阈值编辑
- 将验证脚本纳入更完整的自动化流程
- 补充标准化工程结构，例如 `package.json`、脚本命令和依赖声明

## 一句话结论

这是一个以工业测试分析软件为主题的 Three.js 前端可视化样例，重点在于高密度实验室 UI 与 3D waterfall 图的结合展示；它已经具备本地运行与基础自动验证能力，但当前仍然属于偏原型/演示性质的实现。
